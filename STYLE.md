# 스타일 가이드 (CSS 여백·폰트 규칙)

이 블로그의 스타일은 [`src/styles/global.css`](src/styles/global.css) 한 파일에서 관리합니다.
여백이나 폰트 크기를 새로 지정할 때 이 문서의 규칙을 따르면 사이트 전체 리듬이 흐트러지지 않습니다.

## 기준값

- **`1rem = 16px`** (브라우저 기본, `html`의 폰트 크기를 건드리지 않음)
- 변환: `px ÷ 16 = rem`

| px | rem |
|----|-----|
| 8px | `0.5rem` |
| 12px | `0.75rem` |
| 16px | `1rem` |
| 20px | `1.25rem` |
| 24px | `1.5rem` |
| 32px | `2rem` |
| 40px | `2.5rem` |
| 64px | `4rem` |

## 단위 선택 규칙

> **웬만하면 다 `rem`. "이 요소 글자 크기에 비례해야 해"일 때만 `em`.**

| 단위 | 기준 | 언제 쓰나 |
|------|------|-----------|
| `rem` | root(`html`) = 16px, **항상 고정** | 여백·간격, 대부분의 폰트 크기 |
| `em` | **그 요소 자신의** 폰트 크기, 상대적 | 인라인 코드·버튼 안쪽 padding처럼 글자에 비례해야 하는 것 |
| `px` | 절대 고정 | 진짜 안 변해야 하는 것 (border 1px 등) |

판단이 헷갈리면 자문: **"이 값이 글자 크기 따라 변해야 하나?"** → 변해야 하면 `em`, 아니면 `rem`.

## 간격 스케일 (여백은 이 안에서 골라 쓰기)

새 여백이 필요하면 애매한 값(예: 23px)을 만들지 말고, 아래 스케일에서 가장 가까운 걸 재사용합니다.
(0.25rem = 4px 배수로 맞추면 깔끔합니다.)

```
0.4rem  (6.4px)   아주 작은 간격 (태그 사이)
0.75rem (12px)    작은 간격
1rem    (16px)    기본
1.25rem (20px)    살짝 큰
1.5rem  (24px)    문단 간격
2rem    (32px)    섹션 구분
2.5rem  (40px)    큰 여백
4rem    (64px)    아주 큰 여백
```

## 폰트 크기 스케일 (전부 `rem`, 본문 1rem 기준)

```
0.8rem  (12.8px)  태그, 캡션처럼 작은 글씨
0.9rem  (14.4px)  날짜, 코드
1rem    (16px)    기본 UI 텍스트
1.05rem (16.8px)  본문 (.prose, 살짝 크게)
```

제목을 직접 지정한다면:

```css
h1 { font-size: 2rem; }    /* 32px */
h2 { font-size: 1.5rem; }  /* 24px */
h3 { font-size: 1.25rem; } /* 20px */
```

## `em`을 쓰는 실제 예시

현재 `global.css`에서 `em`을 쓰는 곳은 인라인 코드 하나뿐입니다.

```css
.prose :not(pre) > code {
  font-size: 0.9em;      /* 부모 글자의 90% */
  padding: 0.15em 0.4em; /* "이 90% 크기"에 비례한 여백 → 글자가 작아지면 박스도 같이 작아짐 */
}
```

글자에 딱 붙는 박스가 아니면 `em` 쓸 일은 거의 없습니다.

## 왜 이렇게 하나 (배경)

- **레이아웃 너비는 제한 + 가운데 정렬.** `body`는 화면을 꽉 채우되, 본문 `.container`는 `max-width: 720px`로 좁혀 가독성 확보 (한 줄이 너무 길지 않게).
- **여백을 `rem`으로.** 사용자가 브라우저 기본 폰트를 키우면(접근성) 여백도 같이 커져 비율이 유지됨. `px`면 글자만 커지고 여백은 그대로라 답답해짐.
- **CSS 변수 + 다크모드.** 색은 `--bg`, `--fg` 등 변수로 정의하고 `prefers-color-scheme: dark`로 OS 다크모드를 자동 감지.

## 요약 (이것만 기억)

1. 여백·폰트 → 전부 `rem`. px 정하고 ÷16.
2. 새 값은 위 스케일에서 가까운 걸 재사용.
3. `em`은 인라인 코드·버튼 안쪽 padding처럼 "글자에 비례" 상황만.
4. `border`, 아주 얇은 선 → `px` 그대로.
